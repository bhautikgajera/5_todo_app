import 'package:flutter/material.dart';

class OverViewErrorState extends StatelessWidget {
  const OverViewErrorState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text("ERROR, please try again"),
    );
  }
}
